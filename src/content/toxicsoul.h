#ifndef TOXICSOUL_H
#define TOXICSOUL_H

const char* ToxicSoul[] = {
"就算睡得晚，不会找你的人还是不会找你！",
"今天的事不用放在今天做，万一明天死了呢，就可以不用做了。",
"很多时候，乐观的态度和好听的话帮不了你。",
"狗是人类最好的朋友。 然而狗最好的朋友是「屎」。"
};

// 计算数组元素数量
const int ToxicSoulCount = sizeof(ToxicSoul) / sizeof(ToxicSoul[0]);

#endif // TOXICSOUL_H
